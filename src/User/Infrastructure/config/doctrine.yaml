# User Module - Doctrine ORM Configuration
#
# Defines entity mapping for User bounded context.

doctrine:
    orm:
        mappings:
            User:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/User/Domain'
                prefix: 'App\User\Domain'
                alias: User
